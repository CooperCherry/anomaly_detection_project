# anomaly_detection_project
This project explores the use of unsupervised clustering algorithms (K-means and DBSCAN) to identify anomalous patterns in network traffic. Instead of relying on labeled attack data, the system learns the structure of normal network behavior and flags deviations as potential intrusions.
